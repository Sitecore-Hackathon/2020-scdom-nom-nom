ARG REGISTRY
ARG WINDOWSSERVERCORE_VERSION
ARG SITECORE_VERSION

FROM ${REGISTRY}sitecore-xp-sxa-standalone:${SITECORE_VERSION}-windowsservercore-${WINDOWSSERVERCORE_VERSION}

WORKDIR /inetpub/wwwroot
COPY . .
