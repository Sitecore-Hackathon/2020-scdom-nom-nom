ARG REGISTRY
ARG WINDOWSSERVERCORE_VERSION
ARG SITECORE_VERSION

FROM ${REGISTRY}sitecore-xm-sxa-jss-cm:${SITECORE_VERSION}-windowsservercore-${WINDOWSSERVERCORE_VERSION}

WORKDIR /inetpub/wwwroot
COPY . .
